[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "postgresql_15"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 main:app"
